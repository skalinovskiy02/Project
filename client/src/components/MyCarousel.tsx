import React, {FC} from 'react';

interface MyCarouselProps{
    photos: string[],
}

const MyCarousel: FC<MyCarouselProps> = ({photos}) => {
    return (
        <div>

        </div>
    );
};

export default MyCarousel;